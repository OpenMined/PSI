[package]
name = "psi"
version = "0.1.0"
authors = ["Daniel"]
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
libc = ">=0.2.0"
protobuf = ">=2.0"

[build-dependencies]
protoc-rust = ">=2.0"
